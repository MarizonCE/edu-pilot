待优化点：

1. 支持删除/禁用文档：不仅能导入，还要能从知识库里移除。能做版本迭代。
2. 文件存储到 OSS，MySQL 存文件元数据，用于复用。
3. 可以清洗文件名，避免路径和特殊字符问题。
4. 内存占用可能有点大，进行优化并做超时处理。
5. 豆包生成的文件可能无法通过 MIME 校验，因为其 MIME 类型是 application/octet-stream，需要拆开来检验。
6. 目前未处理 md 文件中的表格（包括 HTML 表格和原生表格），表格中的文字如果使用了标点符号后续进行切块时可能会被切开。
7. 并发方面未作限制处理。
8. 如果图片上传 OSS 失败，需要排查原因和做出处理。未测试会失败的情况。
9. 未对引用式的 md 图片做处理。
10. file_title 直接作为 OSS 的目录名，可能会存在同名的情况导致覆盖先前的文件。
11. 使用网络地址的 md 文件的图片未作处理，更适合 MinerU 解析后的 md 文件。对于 md 文件中的图片在本地的情况，上传时图片不会跟着上传。
12. service_understand_image_and_table.py 里面有函数使用了异步的方式，如果外层函数也运行在事件循环中，也会导致 RuntimeError
13. 